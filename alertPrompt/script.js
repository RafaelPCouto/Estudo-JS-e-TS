// Métodos navegador

const ola = window.alert('Olá, mundo.');
const aceite = window.confirm('Deseja aceitar?');
const promptData = window.prompt('Insira seu nome:');

console.log(ola);
console.log(aceite);
console.log(promptData);

const num1 = window.prompt('Digite um número:');
const num2 = window.prompt('Digite um número:');

const resultado = Number(num1) + Number(num2);

alert(`${resultado}`);

console.log(resultado);